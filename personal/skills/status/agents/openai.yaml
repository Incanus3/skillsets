interface:
  display_name: "Workstream Status"
  short_description: "Summarize a recorded or recently listed workstream"
  default_prompt: "Use $status to report a workstream's recorded or recently listed state."

policy:
  allow_implicit_invocation: false
