---
name: one-by-one
description: Address previously discussed points one at a time without assuming prior output was read. Manual invocation only: apply only if the user explicitly invokes `$one-by-one`; never select automatically from a natural-language request.
---

Address the discussed points one at a time. Assume the user has not read any previous output.
